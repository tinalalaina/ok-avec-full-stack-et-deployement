Travaux DevOps terminés dans ce passage:

1) Sécurité configuration backend
- ALLOWED_HOSTS n'utilise plus '*', il est piloté par variable d'environnement.
- Ajout des paramètres de sécurité HTTP/cookies/HSTS configurables.

2) Gestion des secrets
- Suppression de l'utilisation du fichier legacy backend/env dans settings.
- Ajout d'un backend/.env.example propre (sans secrets).
- Ajout frontend/.env.example.

3) CI/CD qualité
- Ajout d'un workflow CI complet .github/workflows/ci.yml:
  - backend: install + manage.py check + tests + compileall
  - frontend: npm ci + lint + build
  - scan sécurité Trivy (HIGH/CRITICAL)
- Déclenchement sur pull_request et push main.

4) Observabilité minimale
- Ajout endpoint GET /healthz/ retournant {"status":"ok"}.
- Ajout test automatisé de ce endpoint.

5) Exploitation / runbook
- Ajout docs/RUNBOOK.md avec procédures de base (healthcheck, incident, rollback, secrets).

6) Nettoyage de dette précédente
- Suppression du fichier backend/env versionné contenant des secrets.

Notes:
- Le secret déjà exposé historiquement doit être rotaté côté fournisseur.
- Une purge d'historique Git est recommandée si le dépôt est public.
